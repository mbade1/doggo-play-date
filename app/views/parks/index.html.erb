<h1>Dog Parks in Austin</h1>

<p>Click on the name of a park to set up a play date!</p>
<table>
  <tr>
    <th>Name of Park</th>
    <th>Location</th>
    <th>Hound Size Recommendation</th>
  </tr>
  <% @parks.each do |park| %>
    <tr>
      <td><%= link_to "#{park.name}", park_path(park) %></td>
      <td><%= park.location %></td>
      <td><%= park.hound %></td>
    </tr>
  <% end %>
  </table>
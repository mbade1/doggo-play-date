<h1>Dog Parks in Austin</h1>

<p><i>Click on the name of a park to see more play dates and to set up a play date!</i></p>
<table>
  <tr>
    <th>Name of Park</th>
    <th>Location</th>
    <th>Hound Size</th>
  </tr>
  <% @parks.each do |park| %>
    <tr>
      <td><%= link_to "#{park.name}", park_path(park) %></td>
      <td><%= park.location %></td>
      <td><%= park.hound %></td>
    </tr>
  <% end %>
  </table>